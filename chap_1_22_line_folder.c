#include <stdio.h>
#define LINE_SIZE 10

/*
writer a program to "fold" long input  lines into two 
or shorter lines after the last non-blank character that 
occurs before the n-th column of input.

Make sure, your program does something intlligent with very long
lines, and if there are no blanks 
or tabs before the specified column.

*/
int main(void) {
	int ch, counter;
	while ((ch = getchar()) != EOF) {
		counter++
	}
}